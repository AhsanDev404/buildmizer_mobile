import { DefaultTheme, DarkTheme } from 'react-native-paper';

export const CustomTheme = {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: '#E8AA33',
    
  },
};

